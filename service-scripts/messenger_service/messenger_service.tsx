import type { SokolReport } from "../common/_sokolReport.js"


export const listenWindow = () => { }
export const listenElement = (reportData: SokolReport) => { }

export const sendReport = () => { }
